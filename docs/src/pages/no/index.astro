---
import MarketingPage from '../../components/MarketingPage.astro';
import { localeContent, supportedLanguages } from '../../content/site';

const lang = 'no';
const details = localeContent[lang];
---

<MarketingPage lang={lang} details={details} supportedLanguages={supportedLanguages} />
