name: Deploy Demo to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'proxy/public/**'
      - 'pico-bridge/cmake/gen_demo_page.py'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy-pages:
    name: Build and Deploy Demo Page
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Generate demo page
        run: |
          mkdir -p _site
          python3 pico-bridge/cmake/gen_demo_page.py proxy/public/index.html _site/index.html
          cp proxy/public/sw.js _site/sw.js
          cp proxy/public/manifest.json _site/manifest.json
          cp proxy/public/app.js _site/app.js
          cp proxy/public/style.css _site/style.css
          cp proxy/public/icon.svg _site/icon.svg
          cp proxy/public/icon-192.png _site/icon-192.png
          cp proxy/public/icon-512.png _site/icon-512.png
          cp proxy/public/icon-maskable-192.png _site/icon-maskable-192.png
          cp proxy/public/icon-maskable-512.png _site/icon-maskable-512.png
          cp proxy/public/apple-touch-icon.png _site/apple-touch-icon.png
          cp proxy/public/favicon-32.png _site/favicon-32.png
          cp proxy/public/favicon-16.png _site/favicon-16.png

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
